#include <stdio.h>
#include "dog.h"
/**
 * print_dog - a function that prints a struct dog
 * @d: the pointer to be printed.
 */
void print_dog(struct dog *d);
{
    (*d).name = name;
    (*d).age = age;
    (*d).owner = owner;
}
