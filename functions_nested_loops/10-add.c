#include "main.h"
/**
* add -  function that adds two integers and returns the result.
* @a: the 1st integer.
* @b: the 2nd integer.
* Return: show the result.
*/
int add(int a, int b)
{
return (a + b);
}
